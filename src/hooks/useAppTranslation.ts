/**
 * Typed translation hook using the default namespace.
 * Типизированный хук переводов с использованием стандартного пространства имён.
 */
import { useTranslation } from 'react-i18next';

export const useAppTranslation = () => useTranslation();
