import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.talescribe',
  appName: 'Talescribe',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
